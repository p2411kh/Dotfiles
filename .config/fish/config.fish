if status is-interactive
# Commands to run in interactive sessions can go here
   fastfetch
   set -g fish_greeting ""
   alias ls='lsd'
   alias l='ls -l'
   alias ls='ls -a'
   alias lla='ls =la'
   alias lt='ls --tree'

end
